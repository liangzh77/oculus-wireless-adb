# 🎨 中文字体配置 - 超简单版（已修复）

`simsunb.ttf` 已经复制到 `Assets/Fonts/` 目录了！

---

## 📋 只需 4 步操作（更新版）

### 第 1 步：打开字体生成助手

在 Unity 菜单栏：

```
Tools → Create Chinese Font Asset
```

会弹出一个窗口，标题是 "创建中文字体"

---

### 第 2 步：拖拽字体文件

1. **在助手窗口中**，找到 **"1. 源字体文件"** 框

2. **在 Project 窗口中找到字体**：
   ```
   Project → Assets → Fonts → simsunb
   ```

3. **拖拽**：将 `simsunb` 拖拽到助手窗口的框中

---

### 第 3 步：复制字符序列

在助手窗口中，点击 **"复制字符序列到剪贴板"** 按钮

（字符序列会自动复制，包含了所有常用汉字）

---

### 第 4 步：打开并配置 Font Asset Creator

1. **在助手窗口中**，点击 **"3. 打开 Font Asset Creator"** 按钮

2. **会弹出两个窗口**：
   - 一个提示对话框（点击"开始操作"）
   - Font Asset Creator 窗口

3. **在 Font Asset Creator 窗口中配置**：

   **a. 拖拽字体**：
   - **Source Font File**: 拖拽 `Assets/Fonts/simsunb`

   **b. 填写参数**：
   - **Sampling Point Size**: `42`
   - **Padding**: `5`
   - **Packing Method**: `Optimum`
   - **Atlas Resolution**: `2048 x 2048`

   **c. 选择字符集**：
   - **Character Set**: 下拉选择 **`Unicode Range (Hex)`**

   **d. 粘贴字符序列**：
   - **Character Sequence (Hex)**: 点击输入框，按 `Ctrl + V` 粘贴
   - （应该粘贴：`0020-007E,4E00-62FF,6300-77FF,7800-8CFF,8D00-9FA5,3000-303F,FF00-FFEF`）

4. **生成字体**：
   - 点击窗口底部的 **"Generate Font Atlas"** 按钮
   - 等待 2-5 分钟（Unity 会显示进度条）
   - ⚠️ 不要关闭窗口，等待生成完成

5. **保存字体资源**：
   - 生成完成后，点击 **"Save"** 按钮
   - 选择保存位置：`Assets/Fonts/`
   - 文件名：`SimSunB_SDF`
   - 点击保存

---

### 第 5 步：应用中文字体到所有文本

1. **关闭 Font Asset Creator 窗口**

2. **在 Hierarchy 中选中 "ADB UI Setup"**

3. **附加 FixChineseFont 脚本**（如果还没有）：
   - Inspector → Add Component
   - 搜索 `FixChineseFont`
   - 点击添加

4. **拖拽字体资源**：
   - 在 Project 窗口找到 `Assets/Fonts/SimSunB_SDF`
   - 拖拽到 Inspector 中 `FixChineseFont` 组件的 **"Chinese Font Asset"** 字段

5. **应用字体**：
   - 右键 `FixChineseFont` 组件
   - 选择 **"Fix All Chinese Text"**

6. **完成！** 所有中文应该正确显示了

---

## 🎯 详细图示

### Font Asset Creator 窗口配置

```
┌────────────────────────────────────────────┐
│ Font Asset Creator                         │
├────────────────────────────────────────────┤
│ Source Font File                           │
│ ┌────────────────────────────────────────┐ │
│ │ [拖拽 simsunb 到这里]                  │ │ ← 拖拽字体
│ └────────────────────────────────────────┘ │
│                                            │
│ Sampling Point Size    [  42  ]            │ ← 填写 42
│ Padding                [   5  ]            │ ← 填写 5
│ Packing Method         [Optimum ▼]         │
│                                            │
│ Atlas Resolution                           │
│ Width  [ 2048 ]  Height [ 2048 ]           │ ← 填写 2048
│                                            │
│ Character Set     [Unicode Range (Hex) ▼]  │ ← 选择这个
│                                            │
│ Character Sequence (Hex)                   │
│ ┌────────────────────────────────────────┐ │
│ │ [粘贴: 0020-007E,4E00-62FF,...]       │ │ ← Ctrl+V 粘贴
│ └────────────────────────────────────────┘ │
│                                            │
│        [ Generate Font Atlas ]             │ ← 点击生成
│        [      Save      ]                  │ ← 生成后保存
└────────────────────────────────────────────┘
```

---

## ⚠️ 常见问题

### Q1: 编译错误已修复

✅ 脚本已更新，不会再出现 `GlyphRenderMode` 错误

### Q2: Font Asset Creator 窗口找不到

**手动打开：**
```
Unity 菜单 → Window → TextMeshPro → Font Asset Creator
```

### Q3: 生成时提示 "Out of Memory"

**解决方法：**
1. 减小 Atlas Resolution：改为 `1024 x 1024`
2. 或者关闭其他程序释放内存

### Q4: 生成的字体文件找不到

**查找位置：**
- 应该在 `Assets/Fonts/SimSunB_SDF.asset`
- 在 Project 窗口中搜索 "SimSunB_SDF"

### Q5: 某些中文字符还是方框

**原因：** 可能是生僻字，不在字符范围内

**临时方案：** 用常用字替代，或扩大字符范围

---

## 💡 如果觉得麻烦，可以用英文

如果你现在只想快速测试 ADB 功能，可以先用英文：

1. 在 Hierarchy 中选中 "ADB UI Setup"
2. 附加 `FixChineseFont` 脚本
3. 右键组件 → **"Replace Chinese with English"**

中文字体可以之后再慢慢配置！

---

## 📝 完整步骤总结

1. ✅ `Tools` → `Create Chinese Font Asset`
2. ✅ 拖拽 `simsunb` 字体
3. ✅ 点击 "复制字符序列"
4. ✅ 点击 "打开 Font Asset Creator"
5. ✅ 配置参数并粘贴字符序列
6. ✅ 点击 "Generate Font Atlas"（等待 2-5 分钟）
7. ✅ 点击 "Save" 保存为 `SimSunB_SDF`
8. ✅ 拖拽 `SimSunB_SDF` 到 `FixChineseFont` 组件
9. ✅ 右键组件 → "Fix All Chinese Text"

**完成！🎉**
